import { Placeholder } from "@/components/Placeholder";

export default function TurnosPage() {
  return (
    <Placeholder
      title="Turnos"
      description="Elegir plan (2 a 6 días), armar días + horario fijos (schedule_rules) y ver los turnos materializados."
      week={2}
    />
  );
}
